{
  "name": "mcp-server-api",
  "version": "1.0.0",
  "description": "Express server that provides REST API for MCP clients",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "typescript": "^5.3.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "tsx": "^4.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
